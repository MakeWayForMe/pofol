

$(window).scroll( function(){
  /* 2 */
  $('.second_part_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.second2_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });





$(window).scroll( function(){
  /* 2 */
  $('.second_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.second_pioneer_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.second_pioneer_img').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.second_pioneer_img2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.second_pioneer_img3').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-left');}
      else { $(this).removeClass('slide-in-left'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.second_pioneer_img4').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });






















$(window).scroll( function(){
  /* 2 */
  $('.third_part_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });









$(window).scroll( function(){
  /* 2 */
  $('.third_part_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });






$(window).scroll( function(){
  /* 2 */
  $('.third_part_text2_sub').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.third_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });





$(window).scroll( function(){
  /* 2 */
  $('.third_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.third_pioneer_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.third_pioneer_img').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('wiggle');}
      else { $(this).removeClass('wiggle'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.third_pioneer_img2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });






$(window).scroll( function(){
  /* 2 */
  $('.yuilhan_news').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });

















$(window).scroll( function(){
  /* 2 */
  $('.fourth_part_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });









$(window).scroll( function(){
  /* 2 */
  $('.fourth_part_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });






$(window).scroll( function(){
  /* 2 */
  $('.fourth_part_text2_sub').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-left');}
      else { $(this).removeClass('slide-in-left'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });





$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-left');}
      else { $(this).removeClass('slide-in-left'); }  });  });



$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_text3').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_text4').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });





$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_img').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-down');}
      else { $(this).removeClass('slide-in-down'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_img2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_img3').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.fourth_pioneer_img4').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-left');}
      else { $(this).removeClass('slide-in-left'); }  });  });









$(window).scroll( function(){
  /* 2 */
  $('.sixth_part_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });









$(window).scroll( function(){
  /* 2 */
  $('.sixth_part_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });






$(window).scroll( function(){
  /* 2 */
  $('.sixth_part_text2_sub').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.sixth_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });





$(window).scroll( function(){
  /* 2 */
  $('.sixth_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.sixth_pioneer_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.sixth_pioneer_img').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.sixth_pioneer_img2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });











$(window).scroll( function(){
  /* 2 */
  $('.seventh_part_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });









$(window).scroll( function(){
  /* 2 */
  $('.seventh_part_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });






$(window).scroll( function(){
  /* 2 */
  $('.seventh_part_text2_sub').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.seventh_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });





$(window).scroll( function(){
  /* 2 */
  $('.seventh_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.seventh_pioneer_text2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.seventh_pioneer_img').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.seventh_pioneer_img2').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-right');}
      else { $(this).removeClass('slide-in-right'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.eighth_pioneer_text').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });




$(window).scroll( function(){
  /* 2 */
  $('.eighth_pioneer_img').each( function(i){
      var bottom_of_object = $(this).offset().top + $(this).outerHeight()/3;
      var bottom_of_window = $(window).scrollTop() + $(window).height();
      /* 3 */
      if( bottom_of_window > bottom_of_object ){
          $(this).addClass('slide-in-up');}
      else { $(this).removeClass('slide-in-up'); }  });  });







// JavaScript Document